//Movable interface defines a promis for objects that can move
interface Movable {
  // Abstract method for movement
  void move(); // Any class implementing this interface must provide its own move method implementation
}
